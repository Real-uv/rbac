<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cloud.topdaddy.admin.mapper.SysUserMapper">

    <!-- 根据用户名查询用户信息 -->
    <select id="selectByUsername" resultType="cloud.topdaddy.admin.entity.SysUser">
        SELECT * FROM sys_user 
        WHERE username = #{username} AND deleted = 0
    </select>

    <!-- 根据邮箱查询用户信息 -->
    <select id="selectByEmail" resultType="cloud.topdaddy.admin.entity.SysUser">
        SELECT * FROM sys_user 
        WHERE email = #{email} AND deleted = 0
    </select>

    <!-- 根据手机号查询用户信息 -->
    <select id="selectByPhone" resultType="cloud.topdaddy.admin.entity.SysUser">
        SELECT * FROM sys_user 
        WHERE phone = #{phone} AND deleted = 0
    </select>

    <!-- 查询用户的权限列表 -->
    <select id="selectUserPermissions" resultType="java.lang.String">
        SELECT DISTINCT p.code
        FROM sys_permission p
        INNER JOIN sys_role_permission rp ON p.id = rp.permission_id
        INNER JOIN sys_user_role ur ON rp.role_id = ur.role_id
        WHERE ur.user_id = #{userId} 
          AND p.status = 1 
          AND p.deleted = 0
    </select>

    <!-- 查询用户的角色列表 -->
    <select id="selectUserRoles" resultType="java.lang.String">
        SELECT r.code
        FROM sys_role r
        INNER JOIN sys_user_role ur ON r.id = ur.role_id
        WHERE ur.user_id = #{userId} 
          AND r.status = 1 
          AND r.deleted = 0
    </select>

    <!-- 查询用户的角色ID列表 -->
    <select id="selectUserRoleIds" resultType="java.lang.Long">
        SELECT ur.role_id
        FROM sys_user_role ur
        INNER JOIN sys_role r ON ur.role_id = r.id
        WHERE ur.user_id = #{userId} 
          AND r.status = 1 
          AND r.deleted = 0
    </select>

    <!-- 根据角色ID查询用户列表 -->
    <select id="selectUsersByRoleId" resultType="cloud.topdaddy.admin.entity.SysUser">
        SELECT u.*
        FROM sys_user u
        INNER JOIN sys_user_role ur ON u.id = ur.user_id
        WHERE ur.role_id = #{roleId} 
          AND u.deleted = 0
        ORDER BY u.create_time DESC
    </select>

    <!-- 更新用户最后登录信息 -->
    <update id="updateLastLoginInfo">
        UPDATE sys_user 
        SET last_login_time = #{loginTime}, 
            last_login_ip = #{loginIp},
            update_time = NOW()
        WHERE id = #{userId}
    </update>

</mapper>