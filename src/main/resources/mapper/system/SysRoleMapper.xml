<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cloud.topdaddy.admin.mapper.SysRoleMapper">

    <!-- 根据角色编码查询角色信息 -->
    <select id="selectByCode" resultType="cloud.topdaddy.admin.entity.SysRole">
        SELECT * FROM sys_role 
        WHERE code = #{code} AND deleted = 0
    </select>

    <!-- 查询角色的权限ID列表 -->
    <select id="selectRolePermissionIds" resultType="java.lang.Long">
        SELECT rp.permission_id
        FROM sys_role_permission rp
        INNER JOIN sys_permission p ON rp.permission_id = p.id
        WHERE rp.role_id = #{roleId} 
          AND p.status = 1 
          AND p.deleted = 0
        ORDER BY p.sort ASC
    </select>

    <!-- 根据用户ID查询角色列表 -->
    <select id="selectRolesByUserId" resultType="cloud.topdaddy.admin.entity.SysRole">
        SELECT r.*
        FROM sys_role r
        INNER JOIN sys_user_role ur ON r.id = ur.role_id
        WHERE ur.user_id = #{userId} 
          AND r.status = 1 
          AND r.deleted = 0
        ORDER BY r.sort ASC
    </select>

    <!-- 根据权限ID查询角色列表 -->
    <select id="selectRolesByPermissionId" resultType="cloud.topdaddy.admin.entity.SysRole">
        SELECT r.*
        FROM sys_role r
        INNER JOIN sys_role_permission rp ON r.id = rp.role_id
        WHERE rp.permission_id = #{permissionId} 
          AND r.status = 1 
          AND r.deleted = 0
        ORDER BY r.sort ASC
    </select>

    <!-- 检查角色是否被用户使用 -->
    <select id="countUsersByRoleId" resultType="int">
        SELECT COUNT(*)
        FROM sys_user_role ur
        INNER JOIN sys_user u ON ur.user_id = u.id
        WHERE ur.role_id = #{roleId} 
          AND u.deleted = 0
    </select>

</mapper>